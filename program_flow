===
This is just a simple text file for different notes/ideas and program design
===

pcap_lookupdevs()
pcap_open_live()
pcap_loop()
pcap_datalink(handle)		-- to check if device supports datalink layer
pcap_compile()		--	to setup net filtering(MAC/IP) in our case
pcap_setfilter()


check options		--	getopt()
check devices

pcap_loop
	get_addr(buf, mac1, mac2, (*f_mac))		
	get_addr(buf, ip1, ip2, (*f_ip))

	fmac(buf, mac1, mac2);
	fip(buf, ip1, ip2);
